import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Kurniadi Ilham | DevSecOps & Platform Engineer",
  description: "Portfolio website of Kurniadi Ilham, a DevSecOps and Platform Engineer",
  keywords: ["DevSecOps", "Platform Engineer", "Cloud", "Kubernetes", "Security", "Infrastructure", "DevOps"],
  authors: [{ name: "Kurniadi Ilham" }],
  creator: "Kurniadi Ilham",
  icons: {
    icon: "/ki-favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased bg-gray-50 text-gray-900 dark:bg-gray-900 dark:text-gray-100`}
      >
        {children}
      </body>
    </html>
  );
}
